# AGENTS.md

This repository's operating rules, governance, and source-of-truth index live in
`CLAUDE.md` — read that file first, regardless of which AI coding agent you are.

Do not duplicate `CLAUDE.md`'s content into this file. If the rules need to
change, change `CLAUDE.md`; this file stays a one-line pointer so there is never
a second copy to drift out of sync.
